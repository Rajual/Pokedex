/// Tipos de button disponibles
enum ButtonType {
  primary, // Azul con fondo
  secondary, // Blanco con borde
  tertiary, // Gris con borde suave
}

/// Tamaños del button
enum ButtonSize {
  small,
  medium,
  large,
}